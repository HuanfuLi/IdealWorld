---
phase: 03-sfc-reliability-fixes
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [server/src/db/schema.ts, server/src/mechanics/orderBook.ts, server/src/orchestration/simulationRunner.ts]
autonomous: true
requirements: [REL-01, REL-02, REL-03]
must_haves:
  truths:
    - "Pending orders in the Order Book are restored after server restart"
    - "Metabolic costs are aggregated for all actions in the agent's weekly queue"
    - "Iteration records and system snapshots (AMM, Treasury) are committed atomically in the DB"
  artifacts:
    - path: "server/src/db/schema.ts"
      provides: "order_book table definition"
    - path: "server/src/mechanics/orderBook.ts"
      provides: "Persistence logic for orders"
---

<objective>
Implement persistent storage for the Order Book and aggregate metabolic billing across multi-action queues. 
Ensure system snapshots are atomic to prevent data corruption.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/RESEARCH.md
@.planning/phases/03-sfc-reliability-fixes/03-RESEARCH.md
@.planning/phases/03-sfc-reliability-fixes/03-01-SUMMARY.md

<interfaces>
From server/src/mechanics/orderBook.ts:
```typescript
interface Order {
  id: string;
  agentId: string;
  side: 'buy' | 'sell';
  itemType: string;
  price: number;
  quantity: number;
  status: 'open' | 'filled' | 'cancelled';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persistent Order Book Schema and Logic</name>
  <files>server/src/db/schema.ts, server/src/mechanics/orderBook.ts</files>
  <action>
    - Add `order_book` table to `server/src/db/schema.ts` with relevant fields (id, sessionId, agentId, side, itemType, price, quantity, status, createdAt).
    - Modify `server/src/mechanics/orderBook.ts` to replace/augment in-memory `Map` with DB queries.
    - Ensure `matchOrders()` loads 'open' orders from the DB and updates them to 'filled' or 'cancelled' status atomically.
    - Restore session orders in `simulationRunner.ts` during simulation initialization.
  </action>
  <verify>
    <automated>npm test server/src/mechanics/__tests__/physics_sandbox.ts -- -t "orderBook"</automated>
  </verify>
  <done>Order Book state persists between server restarts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-Action MET Aggregation and Atomic Snapshots</name>
  <files>server/src/orchestration/simulationRunner.ts, server/src/mechanics/physicsEngine.ts</files>
  <behavior>
    - Test (MET): Agent with 3 'WORK' actions should burn more satiety than 1 'WORK' action.
    - Test (Snapshot): Verify DB state does not contain partial snapshot data after failure.
  </behavior>
  <action>
    - In `simulationRunner.ts`, locate `applyMETMetabolism`. 
    - Modify it to iterate through `state.executedActions` and sum the metabolic costs for ALL actions in the queue (BUG-06).
    - In the snapshot persistence section of `simulationRunner.ts`, wrap the `iterationsTable` insert and any AMM/Treasury snapshot updates in a single `db.transaction()` (BUG-05).
  </action>
  <verify>
    <automated>npm test server/src/mechanics/__tests__/physics_sandbox.ts -- -t "metabolism"</automated>
  </verify>
  <done>Metabolic billing is accurate for multi-action queues and system snapshots are atomic.</done>
</task>

</tasks>

<verification>
Start a simulation, place several buy/sell orders, restart the server, and verify orders are still present.
</verification>

<success_criteria>
- Pending orders survive simulation pause/resume and server restart.
- Caloric burn scales with the number of high-MET actions in the agent's queue.
</success_criteria>

<output>
After completion, create `.planning/phases/03-sfc-reliability-fixes/03-02-SUMMARY.md`
</output>
