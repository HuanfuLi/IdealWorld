---
phase: 03-sfc-reliability-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [server/src/orchestration/simulationRunner.ts, server/src/db/repos/agentRepo.ts]
autonomous: true
requirements: [SFC-01, SFC-02, SFC-03, SFC-04]
must_haves:
  truths:
    - "Victims of EMBEZZLE who die in the same turn have their negative wealth delta captured before death seizure"
    - "HELP actions do not destroy fiat when agents have zero or low wealth"
    - "Treasury is seeded before genesis wealth floor calculations are performed"
    - "Wealth redistribution uses integer-safe pro-rata logic with remainder distribution"
  artifacts:
    - path: "server/src/orchestration/simulationRunner.ts"
      provides: "Hardened accounting logic"
  key_links:
    - from: "server/src/orchestration/simulationRunner.ts"
      to: "EMBEZZLE settlement"
      via: "wealthDelta capture"
---

<objective>
Harden the core Stock-Flow Consistency (SFC) and accounting logic in the simulation runner.
This plan addresses critical bugs where fiat is created or destroyed due to edge cases in agent death, 
low-wealth actions, and rounding errors.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/RESEARCH.md
@.planning/phases/03-sfc-reliability-fixes/03-RESEARCH.md
@REVIEWS.md

<interfaces>
From server/src/orchestration/simulationRunner.ts:
```typescript
interface AgentWeekState {
  id: string;
  wealth: number;
  wealthDelta: number;
  isAlive: boolean;
  // ... other fields
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix EMBEZZLE Victim-Death and HELP Fiat Destruction</name>
  <files>server/src/orchestration/simulationRunner.ts</files>
  <behavior>
    - Test 1 (EMBEZZLE): Victim wealth should include negative delta before final seizure upon death.
    - Test 2 (HELP): Helper wealth cannot go below zero; fiat must be preserved in the system.
  </behavior>
  <action>
    - In `simulationRunner.ts`, locate the EMBEZZLE action resolution.
    - Per BUG-01: Ensure that if a victim dies, the amount stolen from them (their `wealthDelta`) is subtracted from their wealth BEFORE the final death seizure (which resets wealth to zero and distributes it).
    - Per BUG-03: In the HELP action logic, check the helper's wealth before subtraction. If wealth < help_amount, cap the help_amount to available wealth to prevent negative wealth / fiat destruction.
  </action>
  <verify>
    <automated>npm test server/src/mechanics/__tests__/physics_sandbox.ts -- -t "SFC"</automated>
  </verify>
  <done>EMBEZZLE and HELP actions maintain SFC even in death/bankruptcy edge cases.</done>
</task>

<task type="auto">
  <name>Task 2: Reorder Treasury Seeding and Implement Integer-Safe Rounding</name>
  <files>server/src/orchestration/simulationRunner.ts, server/src/db/repos/agentRepo.ts</files>
  <action>
    - In `simulationRunner.ts`, locate the simulation initialization block.
    - Per BUG-04: Move treasury seeding logic BEFORE any calls to `genesisWealthFloor` or `UBI` calculations.
    - Per BUG-12/14: Audit all `Math.round` or direct division in pro-rata logic (e.g., TAX, UBI, EMBEZZLE). 
    - Implement a helper `distributeProRata(total, ratios)` that uses `Math.floor` for each share and distributes the integer `remainder` to the first N participants to ensure `Sum(Shares) === Total`.
  </action>
  <verify>
    <automated>npm test server/src/mechanics/__tests__/physics_sandbox.ts -- -t "rounding"</automated>
  </verify>
  <done>Treasury is seeded correctly and rounding drift is eliminated from economic transfers.</done>
</task>

</tasks>

<verification>
Run a 10-iteration simulation with high-frequency EMBEZZLE actions and verify that total system wealth remains constant (SFC audit).
</verification>

<success_criteria>
- Total system wealth (Agents + Treasury + AMM) remains constant in edge cases (victim death, zero-wealth help).
- No floating point drift in wealth transfers.
</success_criteria>

<output>
After completion, create `.planning/phases/03-sfc-reliability-fixes/03-01-SUMMARY.md`
</output>
